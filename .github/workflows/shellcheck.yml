name: ShellCheck

on:
  pull_request:
    paths: "**.sh"
  push:
    paths: "**.sh"

permissions:
  contents: read

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: find . -type f -name "*.sh" -exec shellcheck {} +
